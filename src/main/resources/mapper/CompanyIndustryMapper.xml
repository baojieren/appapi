<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gmsj.dao.CompanyIndustryMapper">

    <select id="findIndustryIdByCompanyId" parameterType="integer" resultType="integer">
        select industry_id from company_industry where company_id = #{companyId}
    </select>
</mapper>